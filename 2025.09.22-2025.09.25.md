[toc]
***

### 大模型

gemini 2.5 pro

### 问题

根据以下日报信息，生成 09.22 - 09.25 这周的周报：

1. 
智能移动机器人小组（聂金词）09月22日工作汇报如下：
研发类型：软件开发

完成任务1: 双臂机器人开发
    1. 新增api：
        - get_teachin_file_list() - 获取示教文件列表
        - get_teachin_file();     - 获取示教文件
        - push_teachin_points()   - 推送示教点位数据到服务器
        - get_teachin_points();   - 获取示教点位数据

    2. 同步更新api开发文档

完成任务：修复一个通信库死锁问题

明天计划：

任务1：双臂机器人开发
    - 同步点动控制即时动作的改动
    - 待gui侧，决策侧，机器人侧一起联调

任务2：任务链支持延迟任务/动作


2. 
智能移动机器人小组（聂金词）09月23日工作汇报如下：
研发类型：软件开发

完成任务1: 双臂机器人开发
    1. 同步【单轴联动即时动作】参数的改动
    2. 任务支持延迟（对应场景：在节点等待，等待完成后继续移动到下一个节点）

完成任务2：服务端代码优化
    - 将多个地方硬编码的字符串路径定义为基类中的常量并回归测试(确保所有相关功能均未受影响)

明天计划：

任务1：双臂机器人开发
    - 待联调

3. 
因台风"桦加沙"，停工一天。

4. 
智能移动机器人小组（聂金词）09月25日工作汇报如下：
研发类型：软件开发

完成任务1: 双臂机器人开发
    1. 联调
        - 点动操作时新增示教模式判断(奉工)
        - 使用机器人【即时动作】
        - 关机运动【任务动作】

明天计划：

任务1：双臂机器人开发
    - 继续联调
---

好的，这是根据您提供的日报生成的 09.22 - 09.25 周报。

---

### **智能移动机器人小组周报 (聂金词)**

**汇报周期**：2025.09.22 - 2025.09.25

#### **本周工作总结**

本周核心工作围绕双臂机器人的功能开发与联调展开。主要完成
了示教功能的系列API开发、运动控制的参数同步，并开始与GUI、决策等相关方进行联调测试。同时，任务链系统新增了延迟任务的支持，并修复了一个通信库的死锁问题，提升了服务端的代码质量。周三因台风天气影响，工作暂停一天。

#### **本周完成工作**

**1. 双臂机器人开发**
   - **新增示教功能API：** 为支持机器人示教流程，开发了4个核心API，并同步更新了开发文档。
     - `get_teachin_file_list()`: 获取示教文件列表
     - `get_teachin_file()`: 获取单个示教文件内容
     - `push_teachin_points()`: 上传示教点位数据
     - `get_teachin_points()`: 获取示教点位数据
   - **运动控制功能完善：**
     - 同步了【单轴联动即时动作】的参数改动，确保控制指令的准确性。
     - 在联调过程中，为点动操作新增了【示教模式】的判断逻辑，避免误操作。
   - **任务链支持延迟任务：** 实现了任务链中的延迟等待功能，可满足在指定节点暂停一段时间后再继续执行的业务场景。
   - **多方联合调试：** 与GUI侧、决策侧、机器人侧启动联调工作，共同解决集成中遇到的问题。

**2. 代码优化与问题修复**
   - **修复死锁问题：** 解决了一个通信库中存在的死锁缺陷，提升了系统的稳定性。
   - **服务端代码优化：** 将服务端代码中多处硬编码的路径重构为基类常量，并完成回归测试，增强了代码的可维护性。

**3. 其他**
   - 周三因台风“桦加沙”影响，停工一天。

#### **下周工作计划**

**1. 双臂机器人开发**
   - **持续联调：** 继续与各相关方进行联合调试，重点解决点动控制、示教流程等功能的集成问题，确保整体业务流程顺畅。
   - **跟进新需求与问题：** 根据联调中发现的新问题和需求，进行快速迭代和修复。

---

1. 双臂机器人开发
    - 新增示教功能API： 为支持机器人示教流程，开发了4个API，并同步更新了开发文档。
        - `get_teachin_file_list()`: 获取示教文件列表
        - `get_teachin_file()`: 获取单个示教文件内容
        - `push_teachin_points()`: 上传示教点位数据
        - `get_teachin_points()`: 获取示教点位数据
    - 运动控制功能完善：
        - 同步了【单轴联动即时动作】的参数改动，确保控制指令的准确性。
        - 在联调过程中，为点动操作新增了【示教模式】的判断逻辑，避免误操作。
    - 任务链支持延迟任务 实现了任务链中的延迟等待功能，可满足在指定节点暂停一段时间后再继续执行的业务场景。
    - 多方联合调试：与GUI侧、决策侧、机器人侧启动联调工作，共同解决集成中遇到的问题。

2. 代码优化与问题修复
    - 修复死锁问题： 解决了一个通信库中存在的死锁缺陷，提升了系统的稳定性。
    - 服务端代码优化： 将服务端代码中多处硬编码的路径重构为基类常量，并完成回归测试，增强了代码的可维护性。

3. 其他
   - 周三因台风“桦加沙”影响，停工一天。



    message("CONFIG_INSTALL_DIR is set to ${CONFIG_INSTALL_DIR}")
    set(CONFIG_INSTALL_DIR "${CONFIG_INSTALL_DIR}/${PROJECT_NAME}")

# 外部的 build.sh 会传入 CONFIG_INSTALL_DIR 变量
if("${CONFIG_INSTALL_DIR}" STREQUAL "")
    message("CONFIG_INSTALL_DIR is empty set to /home/byd/config")
    # 设置/home/byd/config/${PROJECT_NAME}"路径
    set(CONFIG_INSTALL_DIR "/home/byd/config/${PROJECT_NAME}")
else()
    # 反之在 CONFIG_INSTALL_DIR 后面添加项目名称作为子目录
    message("CONFIG_INSTALL_DIR is set to ${CONFIG_INSTALL_DIR}/${PROJECT_NAME}")
    set(CONFIG_INSTALL_DIR "${CONFIG_INSTALL_DIR}/${PROJECT_NAME}")
endif()