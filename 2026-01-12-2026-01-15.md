[toc]
***

### 大模型

gemini 3.0 pro

### 问题

根据以下日报信息，生成 01.12 - 01.15 这周的周报：

1. 
智能移动机器人小组（njc）01月12日工作汇报如下：
研发类型：软件开发

完成任务1：上位机建图功能开发
    - 1. 新增【检查在线建图地图文件是否有更新api - check_showmap_update_status】
    - 2. 新增【条件获取在线建图地图文件api - get_showmap_if】
    - 3. 服务端处理程序设计和开发

明天计划：

任务1: 上位机建图功能开发
    - GUI建图涉及的模块: 上位机客户端->上位机服务端->决策->建图和定位
    - 跟奉工对齐跟决策之间的交互(奉工在小漠出差)

任务2: AGV平板app需求(文轩反馈他们目前在做其他的需求)
    - 跟app侧对齐任务链参数(需要贵荣协助)
    - 自测任务链多个场景
    - 联调

2. 
智能移动机器人小组（njc）01月13日工作汇报如下：
研发类型：软件开发

完成任务1：上位机建图功能开发
    - 统一api的响应格式：get_showmap_if 未获取地图文件时，设置响应 code 为-1，把data对象中的状态信息设置到上层的 message 中
    - 新增【设置获取小车位置定时任务间隔api-set_agv_position_interval】

明天计划：

任务1: 上位机建图功能开发
    - GUI建图涉及的模块: 上位机客户端->上位机服务端->决策->建图和定位
    - 跟奉工对齐跟决策之间的交互(奉工在小漠出差)

任务2: AGV平板app需求(文轩反馈他们目前在做其他的需求)
    - 跟app侧对齐任务链参数(需要贵荣协助)
    - 自测任务链多个场景
    - 联调


---

这是一份基于你提供的日报内容整理的周报：

### 智能移动机器人小组周报 (2026.01.12 - 2026.01.15)

**汇报人**：njc
**研发类型**：软件开发

#### 一、 本周工作产出 (Completed Tasks)

**1. 上位机建图功能开发**
本周重点推进了上位机建图功能的服务端开发与接口设计，具体完成情况如下：
*   **服务端程序设计与开发**：完成了核心服务端处理程序的逻辑设计与编码。
*   **API 接口开发与优化**：
    *   新增 `check_showmap_update_status` 接口：用于检查在线建图地图文件是否有更新。
    *   新增 `get_showmap_if` 接口：实现按条件获取在线建图地图文件。
    *   新增 `set_agv_position_interval` 接口：用于设置获取小车位置的定时任务间隔。
    *   **接口规范统一**：优化了 `get_showmap_if` 的响应格式，规范了未获取到地图文件时的错误码（code: -1）及消息反馈机制。

#### 二、 重点待办与下周计划 (Pending & Next Steps)

**1. 上位机建图功能推进**
*   **跨模块对齐**：需跟进上位机客户端、服务端、决策、建图和定位模块的全链路交互逻辑。
*   **协作沟通**：待奉工出差行程确认后，重点对齐与决策模块之间的交互细节。

**2. AGV平板APP需求跟进**
*   **参数对齐**：需与APP侧对齐任务链参数（需贵荣协助）。
*   **测试与联调**：
    *   开展任务链多场景的自测工作。
    *   推进与APP侧的联调测试（目前等待APP侧排期）。

### 周报填写：

#### 一、 本周工作产出 (Completed Tasks)

1. 上位机建图功能开发 本周重点推进了上位机建图功能的服务端开发与接口设计，具体完成情况如下：
- 服务端程序设计与开发：完成了核心服务端处理程序的逻辑设计与编码。
- API 接口开发与优化：
    *   新增 `check_showmap_update_status` 接口：用于检查在线建图地图文件是否有更新。
    *   新增 `get_showmap_if` 接口：实现按条件获取在线建图地图文件。
    *   新增 `set_agv_position_interval` 接口：用于设置获取小车位置的定时任务间隔。

#### 二、 重点待办与下周计划 (Pending & Next Steps)

1. 上位机建图功能推进
跨模块对齐：需跟进上位机客户端、服务端、决策、建图和定位模块的全链路交互逻辑。
协作沟通：待奉工出差行程确认后，重点对齐与决策模块之间的交互细节。

2. AGV平板APP需求跟进
参数对齐：需与APP侧对齐任务链参数（需贵荣协助）。
测试与联调：
    *   开展任务链多场景的自测工作。
    *   推进与APP侧的联调测试（目前等待APP侧排期）。